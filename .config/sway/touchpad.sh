#!/bin/bash



#STATE=`swaymsg -t get_inputs | grep $ID  `

# echo $STATE
# if [ $STATE -eq 1 ]
# then
#     xinput disable $ID
# 		killall fusuma
#     notify-send "Touchpad disabled."
# else
#     xinput enable $ID
# 		notify-send "Touchpad enabled."
# 		fusuma --config=~/.config/fusuma/config.yml  
# fi
